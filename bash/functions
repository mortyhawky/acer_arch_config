#!/usr/bin/env bash

function f_eza() {
    command eza $1 -alh --sort=name --group-directories-first --icons=auto --git
}

function f_cdls() {
  cd $1
  f_eza
}

function f_mkcd() {
  mkdir -p "$1"
  f_cdls $1
}
