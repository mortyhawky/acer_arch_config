#!/usr/bin/env bash

#echo "PATH              = $PATH"
echo "ARCH_VERSION      = $ARCH_VERSION"
echo "BROWSER           = $BROWSER"
echo "BSPC              = $BSPC"
echo "DISPLAY           = $DISPLAY"
echo "EDITOR            = $EDITOR"
echo "GDBINIT           = $GDBINIT"
echo "INPUTRC           = $INPUTRC"
echo "MANPAGER          = $MANPAGER"
echo "SHELL             = $SHELL"
echo "SXHKD_SHELL       = $SXHKD_SHELL"
echo "TERM              = $TERM"
echo "TERMINAL          = $TERMINAL"
echo "W3M_DIR           = $W3M_DIR"
echo "XAUTHORITY        = $XAUTHORITY"
echo "XDG_CONFIG_HOME   = $XDG_CONFIG_HOME"
echo "XDG_DATA_HOME     = $XDG_DATA_HOME"
echo "XDG_STATE_HOME    = $XDG_STATE_HOME"
echo "XINITRC           = $XINITRC"

echo '  ip a '
echo '  sc status |g service           /  rc-status '
echo '  sc start iwd systemd-resolved  /  rc-service iwd start '
echo '  sc disable iwd                 /  rc-update del iwd '
echo '  sc enable  iwd                 /  rc-update add default '
echo '  iwctl station wlan0 connect "103B 5"  '
ip addr show wlan0 |g "inet "

if [[ "$DISPLAY" = ":0" ]]; then
    echo "gui mode"
else
    echo "tty mode"
fi

localectl

#fastfetch
#neofetch
